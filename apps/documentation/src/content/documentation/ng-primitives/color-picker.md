---
name: 'Color Picker'
sourceUrl: 'https://github.com/mgremy/nx_source/tree/main/packages/ng-primitives/color-picker'
primitiveUrl: 'https://angularprimitives.com/primitives/color-picker'
---

# Color Picker

A composable, accessible color picker: a 2D area, channel sliders and hex/channel fields that share
one color value.

> To use `MgnpColorWheel` or `MgnpColorSwatchPicker` with Angular forms (reactive or signal), they
> must be placed inside `MgnpColorPicker`.
>
> If used without `MgnpColorPicker`, you must manually bind each component to its corresponding
> color value (for example a signal containing the selected value).

## Usage

<app-preview name="color-picker"></app-preview>

<app-examples name="color-picker"></app-examples>

## Theme

<app-css-content name="color-picker"></app-css-content>

## Metadata

<app-metadata name="color-picker"></app-metadata>
